<%= form_for @post, html: {name: 'posts', novalidate: true, 'ng-submit' => 'submitForm($event)'} do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, name: 'title', required: true, 'ng-model' => 'post.title' %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, name: 'body', required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
